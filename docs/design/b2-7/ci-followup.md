# B2-7 合并前 CI 修复

记录身份：Codex；适用对象：本批执行者与 Reviewer。User 在候选交付后明确授权合并 PR #23。

首轮远端 CI 在交付 `da01a12` 失败：Python 3.10 为1065通过/3失败/3条件跳过，3.12为1066通过/2失败/3条件跳过。GUI和CodeQL通过。原始输出见[失败日志](ci-first-run-failed.log)，不能把先前本地1070通过当作远端通过。

仅修正四处测试条件，产品实现、锁文件、协议和候选二进制未变：

- 插件崩溃测试：等待首个子进程被回收（5秒上限）后，再调用Host验证显式拉起新进程。管道关闭可能先于asyncio更新returncode；不增加产品自动重放，不删除进程替换/ready断言。
- 鉴权测试：未来nbf/iat改为每个测试执行时计算，不在收集阶段提前计算。25分钟套件中，收集时的“10分钟后”会成为合法过去时间；仍要求所有无效claims返回401。
- CLI测试：只去除Rich ANSI样式后匹配完整旧选项名，保留exit code 2与升级拒绝检查。
- 跨实例取消测试：使用服务默认15秒活跃租约，避免另一实例同步初始化超过原1秒后触发合法过期；heartbeat仍0.01秒，取消、幂等、所有Explorer停止与无Coder启动断言保留。专门的过期及未知写入人工核对用例未变。

[定向回归](ci-portability-targeted.log)共27项通过；Ruff格式/检查、diff检查通过。代码摘要和原始远端统计见[ci-portability.json](ci-portability.json)。独立增量结论见[review.md](review.md)。合并仍须新版远端必需检查全部通过，并匹配已审查的精确HEAD；不绕过分支保护。

此前候选包/真实模型/桌面证据按产品源码不变复用，不因测试修复重跑真实调用或重建相同二进制。首轮失败证据保留。仓库未启用自动合并；本轮不会为此修改仓库设置。
