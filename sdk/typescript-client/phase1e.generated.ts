/* eslint-disable */
// GENERATED FILE - DO NOT EDIT. Source: sdk/protocol/schema/operant-phase1e.openapi.json
// Generated by sdk/protocol/generate_phase1e.py; schema digest: 10bb7cf130c0dc9b183da7915faf3900dc133ed9983e1ab022460150cfabc5f3

import {
  Phase1EError,
  Phase1ERequest,
  Phase1EResponse,
  Phase1ETransport,
  fetchPhase1ETransport,
  parseCursor,
  parseSse,
  readJson,
  readText,
  responseHeaders,
  stringifyJson,
} from './phase1e-transport';

export const PHASE1E_PROTOCOL_VERSION = "phase1e.v1" as const;
export const PHASE1E_SCHEMA_DIGEST = "10bb7cf130c0dc9b183da7915faf3900dc133ed9983e1ab022460150cfabc5f3" as const;
export const PHASE1E_MAX_CURSOR = 9223372036854775807n;

/** All int64 values remain a safe number or a bigint; JSON parsing promotes large tokens. */
export type Int64 = number | bigint;
export type Cursor = Int64;

export interface ProtocolNegotiation {
  protocol_version: "phase1e.v1";
  schema_digest: string;
  min_client_version: "phase1e.v1";
  capabilities: Array<string>;
}

export type ErrorRecovery = "none" | "retry" | "retry_later" | "retry_same_idempotency_key" | "use_new_idempotency_key" | "refresh_and_retry" | "manual_reconcile";

export interface ErrorDescriptor {
  code: string;
  message: string;
  retryable: boolean;
  recovery: ErrorRecovery;
}

export interface ErrorEnvelope {
  detail: unknown;
  error: ErrorDescriptor;
  [key: string]: unknown;
}

export interface ReceiptResource {
  type: string;
  id: string;
}

export interface CommandReceipt {
  command_kind: "command" | "stream";
  accepted: boolean;
  command_id: string;
  stream_replay_available?: boolean;
  first_event_type?: string | null;
  resource_type?: string | null;
  resource_id?: string | null;
  replay_url?: string | null;
  replay_after_cursor?: Cursor | null;
  recovery?: "replay_events" | "not_available" | "none" | "manual_reconcile";
  resource?: ReceiptResource | null;
  [key: string]: unknown;
}

export interface ProjectThreadSummary {
  id: string;
  status: string;
  created_at: string;
  updated_at: string;
}

export interface ProjectWorkflowRunSummary {
  id: string;
  status: string;
  current_stage: string;
  summary: string;
  created_at: string;
  updated_at: string;
}

export interface ProjectProjection {
  project_id: string;
  workspace_ref: string;
  readable: boolean;
  writable: boolean;
  created_at: string;
  threads: Array<ProjectThreadSummary>;
  workflow_runs: Array<ProjectWorkflowRunSummary>;
}

export interface WorkspaceFileEntry {
  path: string;
  name: string;
  type: "file" | "directory";
  size_bytes: Int64 | null;
  modified_at: string | null;
}

export interface WorkspaceFilesPage {
  workspace_id: string;
  path: string;
  entries: Array<WorkspaceFileEntry>;
  snapshot: string;
  next_page_token: string | null;
}

export type ThreadStatus = "active" | "completed" | "cancelled" | "archived";

export interface ThreadLegacyRef {
  source_type: "session" | "workflow_run";
  source_id: string;
}

export interface ThreadProjection {
  id: string;
  cursor: Cursor | null;
  parent_thread_id: string | null;
  workspace_ref: string | null;
  status: ThreadStatus;
  legacy_refs: Array<ThreadLegacyRef>;
  created_at: string;
  updated_at: string;
  archived_at: string | null;
}

export interface RoleSnapshot {
  role_id: string;
  role_version: number;
  role_name: string;
  system_prompt: string;
  model_profile_id: string;
  model_profile_name: string;
  provider: string;
  model_id: string;
  base_url: string;
  secret_ref: string;
  context_window?: number | null;
  effort: string;
  provider_effort_parameter?: string | null;
  provider_effort_value?: string | null;
  tool_policy: Record<string, unknown>;
  budget: Record<string, unknown>;
  memory_scope: string;
  captured_at: string;
  overrides: Record<string, unknown>;
  [key: string]: unknown;
}

export interface Session {
  id: string;
  role_snapshot: RoleSnapshot;
  created_at: string;
}

export interface CreateRole {
  name: string;
  system_prompt: string;
  model_profile_id: string;
  [key: string]: unknown;
}

export interface CreateSessionRequest {
  role_id?: string | null;
  new_role?: CreateRole | null;
  model_profile_id?: string | null;
  effort?: string | null;
  budget_overrides?: Record<string, unknown> | null;
  thread_id?: string | null;
}

export interface ReferenceRequest {
  path?: string;
  start_line?: number;
  end_line?: number;
  [key: string]: unknown;
}

export interface RunSessionRequest {
  message: string;
  workspace: string;
  thread_id?: string | null;
  references?: Array<ReferenceRequest>;
}

export interface RuntimeEvent {
  id?: string;
  cursor?: Cursor | null;
  session_id?: string;
  agent_id?: string | null;
  event_type?: string;
  payload?: Record<string, unknown>;
  created_at?: string;
  [key: string]: unknown;
}

export interface SseFrame {
  id: Cursor | null;
  event: string;
  data: unknown;
  resource_scope?: string;
  stream_kind?: string;
}

export type ApprovalStatus = "pending" | "approved" | "denied" | "expired" | "cancelled";

export interface ApprovalProjection {
  approval_id: string;
  tool_call_id: string;
  category: string;
  detail: string;
  action_hash: string;
  status: ApprovalStatus;
  requested_at: string;
  expires_at: string;
  continuation_available: boolean;
}

export interface ApprovalDecisionRequest {
  approved: boolean;
}

export interface ApprovalDecisionResult {
  accepted: boolean;
  changed: boolean;
  approved: boolean;
  status: ApprovalStatus;
  approval_id: string;
  continuation_available: boolean;
  [key: string]: unknown;
}

type OperationParameter = { name: string; in: string; required: boolean; schema: Record<string, unknown> };
type OperationDefinition = {
  method: string; pathTemplate: string; parameters: readonly OperationParameter[];
  requestBody: Record<string, unknown> | null; mutuallyExclusive: readonly (readonly string[])[];
  responses: Record<string, Record<string, unknown>>;
};

const PHASE1E_OPERATIONS: Record<string, OperationDefinition> = {
  "negotiateProtocol": {
    "method": "GET",
    "pathTemplate": "/v1/protocol",
    "parameters": [],
    "requestBody": null,
    "mutuallyExclusive": [],
    "responses": {
      "200": {
        "description": "Protocol metadata",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ProtocolNegotiation"
            }
          }
        }
      },
      "default": {
        "description": "Typed public error envelope",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorEnvelope"
            }
          }
        }
      }
    }
  },
  "listProjects": {
    "method": "GET",
    "pathTemplate": "/v1/projects",
    "parameters": [
      {
        "name": "after_cursor",
        "in": "query",
        "required": false,
        "schema": {
          "$ref": "#/components/schemas/Cursor"
        }
      },
      {
        "name": "limit",
        "in": "query",
        "required": false,
        "schema": {
          "type": "integer",
          "minimum": 1,
          "maximum": 1000,
          "default": 100
        }
      }
    ],
    "requestBody": null,
    "mutuallyExclusive": [],
    "responses": {
      "200": {
        "description": "Workspace-backed project projections",
        "content": {
          "application/json": {
            "schema": {
              "type": "array",
              "items": {
                "$ref": "#/components/schemas/ProjectProjection"
              }
            }
          }
        }
      },
      "default": {
        "description": "Typed public error envelope",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorEnvelope"
            }
          }
        }
      }
    }
  },
  "listWorkspaceFiles": {
    "method": "GET",
    "pathTemplate": "/v1/workspaces/{workspace_id}/files",
    "parameters": [
      {
        "name": "workspace_id",
        "in": "path",
        "required": true,
        "schema": {
          "type": "string",
          "minLength": 1,
          "maxLength": 300
        }
      },
      {
        "name": "path",
        "in": "query",
        "required": false,
        "schema": {
          "type": "string",
          "minLength": 1,
          "maxLength": 4096,
          "default": "."
        }
      },
      {
        "name": "limit",
        "in": "query",
        "required": false,
        "schema": {
          "type": "integer",
          "minimum": 1,
          "maximum": 200,
          "default": 100
        }
      },
      {
        "name": "page_token",
        "in": "query",
        "required": false,
        "schema": {
          "type": "string",
          "maxLength": 2000
        }
      },
      {
        "name": "snapshot",
        "in": "query",
        "required": false,
        "schema": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        }
      },
      {
        "name": "after_name",
        "in": "query",
        "required": false,
        "schema": {
          "type": "string",
          "maxLength": 255
        }
      }
    ],
    "requestBody": null,
    "mutuallyExclusive": [],
    "responses": {
      "200": {
        "description": "A deterministic metadata page",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/WorkspaceFilesPage"
            }
          }
        }
      },
      "default": {
        "description": "Typed public error envelope",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorEnvelope"
            }
          }
        }
      }
    }
  },
  "listThreads": {
    "method": "GET",
    "pathTemplate": "/v1/threads",
    "parameters": [
      {
        "name": "after_cursor",
        "in": "query",
        "required": false,
        "schema": {
          "$ref": "#/components/schemas/Cursor"
        }
      },
      {
        "name": "limit",
        "in": "query",
        "required": false,
        "schema": {
          "type": "integer",
          "minimum": 1,
          "maximum": 1000,
          "default": 100
        }
      },
      {
        "name": "parent_thread_id",
        "in": "query",
        "required": false,
        "schema": {
          "type": "string",
          "maxLength": 300
        }
      },
      {
        "name": "workspace_ref",
        "in": "query",
        "required": false,
        "schema": {
          "type": "string",
          "maxLength": 2048
        }
      },
      {
        "name": "status",
        "in": "query",
        "required": false,
        "schema": {
          "$ref": "#/components/schemas/ThreadStatus"
        }
      }
    ],
    "requestBody": null,
    "mutuallyExclusive": [],
    "responses": {
      "200": {
        "description": "Canonical thread projections",
        "content": {
          "application/json": {
            "schema": {
              "type": "array",
              "items": {
                "$ref": "#/components/schemas/ThreadProjection"
              }
            }
          }
        }
      },
      "default": {
        "description": "Typed public error envelope",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorEnvelope"
            }
          }
        }
      }
    }
  },
  "createSession": {
    "method": "POST",
    "pathTemplate": "/v1/sessions",
    "parameters": [
      {
        "name": "Idempotency-Key",
        "in": "header",
        "required": false,
        "schema": {
          "type": "string",
          "minLength": 1,
          "maxLength": 300
        }
      }
    ],
    "requestBody": {
      "required": true,
      "content": {
        "application/json": {
          "schema": {
            "$ref": "#/components/schemas/CreateSessionRequest"
          }
        }
      }
    },
    "mutuallyExclusive": [
      [
        "role_id",
        "new_role"
      ]
    ],
    "responses": {
      "201": {
        "description": "Created session",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/Session"
            }
          }
        }
      },
      "default": {
        "description": "Typed public error envelope",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorEnvelope"
            }
          }
        }
      }
    }
  },
  "runSessionStream": {
    "method": "POST",
    "pathTemplate": "/v1/sessions/{session_id}/runs",
    "parameters": [
      {
        "name": "session_id",
        "in": "path",
        "required": true,
        "schema": {
          "type": "string",
          "minLength": 1,
          "maxLength": 300
        }
      },
      {
        "name": "Idempotency-Key",
        "in": "header",
        "required": false,
        "schema": {
          "type": "string",
          "minLength": 1,
          "maxLength": 300
        }
      },
      {
        "name": "Last-Event-ID",
        "in": "header",
        "required": false,
        "schema": {
          "$ref": "#/components/schemas/Cursor"
        }
      }
    ],
    "requestBody": {
      "required": true,
      "content": {
        "application/json": {
          "schema": {
            "$ref": "#/components/schemas/RunSessionRequest"
          }
        }
      }
    },
    "mutuallyExclusive": [],
    "responses": {
      "200": {
        "description": "New or replayed committed SSE events",
        "content": {
          "text/event-stream": {
            "schema": {
              "$ref": "#/components/schemas/SseFrame"
            }
          }
        }
      },
      "202": {
        "description": "Typed receipt returned for an idempotency replay",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/CommandReceipt"
            }
          }
        }
      },
      "default": {
        "description": "Typed public error envelope",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorEnvelope"
            }
          }
        }
      }
    }
  },
  "listPendingApprovals": {
    "method": "GET",
    "pathTemplate": "/v1/sessions/{session_id}/approvals",
    "parameters": [
      {
        "name": "session_id",
        "in": "path",
        "required": true,
        "schema": {
          "type": "string",
          "minLength": 1,
          "maxLength": 300
        }
      }
    ],
    "requestBody": null,
    "mutuallyExclusive": [],
    "responses": {
      "200": {
        "description": "Pending approval projections",
        "content": {
          "application/json": {
            "schema": {
              "type": "array",
              "items": {
                "$ref": "#/components/schemas/ApprovalProjection"
              }
            }
          }
        }
      },
      "default": {
        "description": "Typed public error envelope",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorEnvelope"
            }
          }
        }
      }
    }
  },
  "submitApproval": {
    "method": "POST",
    "pathTemplate": "/v1/sessions/{session_id}/approvals/{tool_call_id}",
    "parameters": [
      {
        "name": "session_id",
        "in": "path",
        "required": true,
        "schema": {
          "type": "string",
          "minLength": 1,
          "maxLength": 300
        }
      },
      {
        "name": "tool_call_id",
        "in": "path",
        "required": true,
        "schema": {
          "type": "string",
          "minLength": 1,
          "maxLength": 300
        }
      },
      {
        "name": "Idempotency-Key",
        "in": "header",
        "required": false,
        "schema": {
          "type": "string",
          "minLength": 1,
          "maxLength": 300
        }
      }
    ],
    "requestBody": {
      "required": true,
      "content": {
        "application/json": {
          "schema": {
            "$ref": "#/components/schemas/ApprovalDecisionRequest"
          }
        }
      }
    },
    "mutuallyExclusive": [],
    "responses": {
      "200": {
        "description": "Persisted approval decision result",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ApprovalDecisionResult"
            }
          }
        }
      },
      "default": {
        "description": "Typed public error envelope",
        "content": {
          "application/json": {
            "schema": {
              "$ref": "#/components/schemas/ErrorEnvelope"
            }
          }
        }
      }
    }
  }
} as const;

export interface ResponseMetadata {
  status: number; idempotencyKey?: string; idempotencyReplayed: boolean;
}
export interface Phase1ERequestOptions { idempotencyKey?: string; }
export interface ProjectListOptions {
  afterCursor?: Cursor;
  limit?: number;
}

export interface WorkspaceFilesOptions {
  path?: string;
  limit?: number;
  pageToken?: string;
  snapshot?: string;
  afterName?: string;
}

export interface ThreadListOptions {
  afterCursor?: Cursor;
  limit?: number;
  parentThreadId?: string;
  workspaceRef?: string;
  status?: ThreadStatus;
}

export interface CreateSessionOptions {
  idempotencyKey?: string;
}

export interface RunSessionStreamOptions extends Phase1ERequestOptions {
  lastEventId?: Cursor;
}

export interface SubmitApprovalOptions {
  idempotencyKey?: string;
}

export interface RunSessionStream {
  receipt: CommandReceipt | null; metadata: ResponseMetadata;
  events: AsyncIterable<SseFrame>;
}

/** Deduplicate only a bounded recent window per resource and stream scope. */
export class ScopedCursorTracker {
  private readonly seen = new Map<string, Set<string>>();
  private readonly order = new Map<string, string[]>();
  private readonly latest = new Map<string, Cursor>();
  private readonly scopeOrder: string[] = [];
  constructor(private readonly maxSeenPerScope = 1024, private readonly maxScopes = 1024) {
    if (!Number.isSafeInteger(maxSeenPerScope) || maxSeenPerScope < 1 || !Number.isSafeInteger(maxScopes) || maxScopes < 1) throw new RangeError('cursor tracker bounds must be positive safe integers');
  }

  accept(frame: SseFrame): boolean {
    if (frame.id === null || frame.id === undefined) return true;
    if (!frame.resource_scope || !frame.stream_kind) throw new TypeError('SSE scope is required');
    const parsed = parseCursor(frame.id);
    const key = `${frame.resource_scope}\0${frame.stream_kind}`;
    const cursor = BigInt(parsed).toString();
    const scope = this.seen.get(key) ?? new Set<string>();
    const order = this.order.get(key) ?? [];
    if (!this.seen.has(key)) { this.scopeOrder.push(key); while (this.scopeOrder.length > this.maxScopes) { const evictedKey = this.scopeOrder.shift(); if (evictedKey !== undefined) { this.seen.delete(evictedKey); this.order.delete(evictedKey); this.latest.delete(evictedKey); } } }
    if (scope.has(cursor)) return false;
    scope.add(cursor); order.push(cursor);
    while (order.length > this.maxSeenPerScope) { const evicted = order.shift(); if (evicted !== undefined) scope.delete(evicted); }
    this.seen.set(key, scope); this.order.set(key, order);
    const previous = this.latest.get(key);
    if (previous === undefined || BigInt(parsed) > BigInt(previous)) this.latest.set(key, parsed);
    return true;
  }

  last(scope: string, streamKind: string): Cursor | null {
    if (!scope || !streamKind) throw new TypeError('SSE scope is required');
    return this.latest.get(`${scope}\0${streamKind}`) ?? null;
  }
}

export class ProtocolNegotiationError extends Phase1EError {
  constructor(message: string) { super('protocol_incompatible', message, false, 'refresh_and_retry'); this.name = 'ProtocolNegotiationError'; }
}

function validateProtocolNegotiation(value: unknown): ProtocolNegotiation {
  if (typeof value !== 'object' || value === null || Array.isArray(value)) throw new ProtocolNegotiationError('protocol response is not an object');
  const metadata = value as Record<string, unknown>;
  const required = ['protocol_version', 'schema_digest', 'min_client_version', 'capabilities'];
  if (required.some(field => !Object.prototype.hasOwnProperty.call(metadata, field))) throw new ProtocolNegotiationError('protocol response is missing required metadata');
  if (metadata.protocol_version !== PHASE1E_PROTOCOL_VERSION || metadata.min_client_version !== PHASE1E_PROTOCOL_VERSION) throw new ProtocolNegotiationError(`unsupported Core protocol: ${String(metadata.protocol_version)}`);
  if (metadata.schema_digest !== PHASE1E_SCHEMA_DIGEST) throw new ProtocolNegotiationError(`Core Schema digest mismatch: expected ${PHASE1E_SCHEMA_DIGEST}, got ${String(metadata.schema_digest)}`);
  if (!Array.isArray(metadata.capabilities) || metadata.capabilities.some(item => typeof item !== 'string' || item.length === 0)) throw new ProtocolNegotiationError('Core protocol capabilities are invalid');
  return metadata as unknown as ProtocolNegotiation;
}

function newIdempotencyKey(): string {
  const cryptoApi = globalThis.crypto;
  if (typeof cryptoApi?.randomUUID === 'function') return cryptoApi.randomUUID();
  if (typeof cryptoApi?.getRandomValues === 'function') {
    const bytes = cryptoApi.getRandomValues(new Uint8Array(16));
    bytes[6] = (bytes[6] & 0x0f) | 0x40; bytes[8] = (bytes[8] & 0x3f) | 0x80;
    const hex = Array.from(bytes, byte => byte.toString(16).padStart(2, '0')).join('');
    return `${hex.slice(0, 8)}-${hex.slice(8, 12)}-${hex.slice(12, 16)}-${hex.slice(16, 20)}-${hex.slice(20)}`;
  }
  throw new Phase1EError('crypto_unavailable', 'Secure idempotency key generation is unavailable', false, 'none');
}

function requireIdempotencyKey(value: string): string {
  if (typeof value !== 'string' || !value) throw new TypeError('idempotencyKey must not be empty');
  return value;
}

function cursorQuery(value: Cursor | undefined): string | undefined {
  if (value === undefined) return undefined;
  return parseCursor(value).toString();
}

function pathPart(value: string, label: string): string {
  if (!value) throw new TypeError(`${label} must not be empty`);
  return encodeURIComponent(value);
}

function validateMutuallyExclusive(request: unknown, groups: readonly (readonly string[])[]): void {
  if (groups.length === 0) return;
  if (typeof request !== 'object' || request === null || Array.isArray(request)) throw new TypeError('request body must be an object');
  const record = request as Record<string, unknown>;
  for (const group of groups) {
    if (!Array.isArray(group) || group.length < 2 || group.some(field => typeof field !== 'string' || field.length === 0)) throw new TypeError('invalid mutually-exclusive schema metadata');
    const provided = group.filter(field => record[field] !== undefined && record[field] !== null);
    if (provided.length !== 1) throw new TypeError(`exactly one of ${group.join(', ')} is required`);
  }
}

export class Phase1EClient {
  readonly protocolVersion = PHASE1E_PROTOCOL_VERSION;
  readonly schemaDigest = PHASE1E_SCHEMA_DIGEST;
  private readonly baseUrl: string;
  private readonly transport: Phase1ETransport;
  private readonly clientVersion: string;
  private negotiated: ProtocolNegotiation | null = null;
  private responseMetadata: ResponseMetadata | null = null;

  constructor(baseUrl = 'http://127.0.0.1:8000', transport: Phase1ETransport = fetchPhase1ETransport, clientVersion = PHASE1E_PROTOCOL_VERSION) {
    this.baseUrl = baseUrl.replace(/\/+$/, ''); this.transport = transport; this.clientVersion = clientVersion;
  }

  get lastResponse(): ResponseMetadata | null { return this.responseMetadata; }
  get negotiatedProtocol(): ProtocolNegotiation | null { return this.negotiated; }

  async negotiateProtocol(force = false): Promise<ProtocolNegotiation> {
    if (this.negotiated && !force) return this.negotiated;
    const response = await this.requestOperation(PHASE1E_OPERATIONS.negotiateProtocol, {}, true);
    const metadata = validateProtocolNegotiation(await readJson<unknown>(response));
    this.negotiated = metadata; return metadata;
  }

  private async requestOperation(operation: OperationDefinition, args: { pathParams?: Record<string, string>; query?: Record<string, string | undefined>; headers?: Record<string, string | undefined>; body?: string; bodyValue?: unknown; }, skipNegotiation = false): Promise<Phase1EResponse> {
    validateMutuallyExclusive(args.bodyValue, operation.mutuallyExclusive);
    const path = operation.pathTemplate.replace(/\{([^}]+)\}/g, (_match, name: string) => { const value = args.pathParams?.[name]; if (value === undefined) throw new TypeError(`missing path parameter: ${name}`); return value; });
    const headers = Object.fromEntries(Object.entries(args.headers ?? {}).filter((entry): entry is [string, string] => entry[1] !== undefined));
    return this.requestRaw({ method: operation.method, path, query: args.query, headers, body: args.body }, skipNegotiation);
  }

  private async requestRaw(request: Omit<Phase1ERequest, 'url'> & { path: string; query?: Record<string, string | undefined>; }, skipNegotiation = false): Promise<Phase1EResponse> {
    if (!skipNegotiation) await this.negotiateProtocol();
    const url = new URL(`${this.baseUrl}${request.path}`); for (const [key, value] of Object.entries(request.query ?? {})) if (value !== undefined) url.searchParams.set(key, value);
    let response: Phase1EResponse;
    try { response = await this.transport({ ...request, url: url.toString(), headers: { 'X-Operant-Client-Version': this.clientVersion, ...(request.headers ?? {}) } }); }
    catch (error: unknown) { if (error instanceof Phase1EError) throw error; throw new Phase1EError('transport_unavailable', error instanceof Error ? error.message : 'transport request failed', true, 'retry_later'); }
    const headers = responseHeaders(response.headers); this.responseMetadata = { status: response.status, idempotencyKey: headers['idempotency-key'], idempotencyReplayed: headers['idempotency-replayed']?.toLowerCase() === 'true' };
    if (response.status < 200 || response.status >= 300) throw await Phase1EError.fromResponse(response); return response;
  }

  async listProjects(options: ProjectListOptions = {}): Promise<Array<ProjectProjection>> {
    const response = await this.requestOperation(PHASE1E_OPERATIONS["listProjects"], { query: { "after_cursor": options.afterCursor === undefined ? undefined : cursorQuery(options.afterCursor), "limit": options.limit === undefined ? undefined : String(options.limit) }, headers: { Accept: "application/json" } });
    return readJson<Array<ProjectProjection>>(response);
  }

  async listWorkspaceFiles(workspaceId: string, options: WorkspaceFilesOptions = {}): Promise<WorkspaceFilesPage> {
    const response = await this.requestOperation(PHASE1E_OPERATIONS["listWorkspaceFiles"], { pathParams: { "workspace_id": pathPart(workspaceId, "workspace_id") }, query: { "path": options.path === undefined ? undefined : String(options.path), "limit": options.limit === undefined ? undefined : String(options.limit), "page_token": options.pageToken === undefined ? undefined : String(options.pageToken), "snapshot": options.snapshot === undefined ? undefined : String(options.snapshot), "after_name": options.afterName === undefined ? undefined : String(options.afterName) }, headers: { Accept: "application/json" } });
    return readJson<WorkspaceFilesPage>(response);
  }

  async listThreads(options: ThreadListOptions = {}): Promise<Array<ThreadProjection>> {
    const response = await this.requestOperation(PHASE1E_OPERATIONS["listThreads"], { query: { "after_cursor": options.afterCursor === undefined ? undefined : cursorQuery(options.afterCursor), "limit": options.limit === undefined ? undefined : String(options.limit), "parent_thread_id": options.parentThreadId === undefined ? undefined : String(options.parentThreadId), "workspace_ref": options.workspaceRef === undefined ? undefined : String(options.workspaceRef), "status": options.status === undefined ? undefined : String(options.status) }, headers: { Accept: "application/json" } });
    return readJson<Array<ThreadProjection>>(response);
  }

  async createSession(request: CreateSessionRequest, options: CreateSessionOptions = {}): Promise<Session> {
    const response = await this.requestOperation(PHASE1E_OPERATIONS["createSession"], { headers: { 'Idempotency-Key': requireIdempotencyKey(options.idempotencyKey ?? newIdempotencyKey()), Accept: "application/json", 'Content-Type': "application/json" }, bodyValue: request, body: stringifyJson(request) });
    return readJson<Session>(response);
  }

  async runSessionStream(sessionId: string, request: RunSessionRequest, options: RunSessionStreamOptions = {}): Promise<RunSessionStream> {
    const response = await this.requestOperation(PHASE1E_OPERATIONS["runSessionStream"], { pathParams: { "session_id": pathPart(sessionId, "session_id") }, headers: { 'Last-Event-ID': options.lastEventId === undefined ? undefined : cursorQuery(options.lastEventId), 'Idempotency-Key': requireIdempotencyKey(options.idempotencyKey ?? newIdempotencyKey()), Accept: "text/event-stream, application/json", 'Content-Type': "application/json" }, bodyValue: request, body: stringifyJson(request) });
    const metadata = this.responseMetadata ?? { status: response.status, idempotencyReplayed: false };
    const contentType = responseHeaders(response.headers)['content-type']?.split(';', 1)[0]?.trim().toLowerCase();
    if (response.status === 202) {
      if (contentType !== 'application/json') throw new Phase1EError('invalid_stream_response', '202 stream receipt must use application/json', false, 'none');
      return { receipt: await readJson<CommandReceipt>(response), metadata, events: (async function*() {})() };
    }
    if (response.status !== 200 || contentType !== 'text/event-stream') throw new Phase1EError('invalid_stream_response', '200 stream response must use text/event-stream', false, 'none');
    const source = response.body ?? await readText(response);
    return { receipt: null, metadata, events: (async function*() {
      for await (const frame of parseSse(source)) {
        const id = frame.id === undefined ? null : parseCursor(frame.id);
        yield { id, event: frame.event ?? 'message', data: frame.data, resource_scope: `session:${sessionId}`, stream_kind: 'session.run' };
      }
    })() };
  }

  async listPendingApprovals(sessionId: string): Promise<Array<ApprovalProjection>> {
    const response = await this.requestOperation(PHASE1E_OPERATIONS["listPendingApprovals"], { pathParams: { "session_id": pathPart(sessionId, "session_id") }, headers: { Accept: "application/json" } });
    return readJson<Array<ApprovalProjection>>(response);
  }

  async submitApproval(sessionId: string, toolCallId: string, request: ApprovalDecisionRequest, options: SubmitApprovalOptions = {}): Promise<ApprovalDecisionResult> {
    const response = await this.requestOperation(PHASE1E_OPERATIONS["submitApproval"], { pathParams: { "session_id": pathPart(sessionId, "session_id"), "tool_call_id": pathPart(toolCallId, "tool_call_id") }, headers: { 'Idempotency-Key': requireIdempotencyKey(options.idempotencyKey ?? newIdempotencyKey()), Accept: "application/json", 'Content-Type': "application/json" }, bodyValue: request, body: stringifyJson(request) });
    return readJson<ApprovalDecisionResult>(response);
  }

}

export { Phase1EError };
