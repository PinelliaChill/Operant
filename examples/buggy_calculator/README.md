# 有缺陷的 calculator 验收项目

这个小项目专门用于 Planner → Coder → Reviewer 的真实模型验收。运行验收前，脚本会把它
复制到临时 Git workspace，Agent 不会直接修改仓库中的原始 fixture。

交给 Agent 的任务：

> 修复除法逻辑：保留小数结果，并在除数为零时给出清楚的错误。不要破坏加法，让全部测试通过。

仓库中保留的是有缺陷版本。每次验收都操作临时副本，保证起点一致。
